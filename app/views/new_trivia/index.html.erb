<div class="container">
  <div class="flash">
    <p id="notice"><%= notice %></p>

    <h1>Listing New Trivia</h1>

    <h4>Title: <%= @response['title'] %></h4>
    <h4>Categories: <%= @response2['topics'] %></h4>
    <div class="row">
      <div class="col-xs-6">
        <h4>OpenIE Relation</h4>
        <table class="table table-striped">
          <thead>
            <tr>
              <th>First Entity</th>
              <th>Relation Type</th>
              <th>Second Type</th>
            </tr>
          </thead>

          <tbody>
            <% @response['openie_relations'].each do |r| %>
              <tr>
                <% r.each do |el| %>
                <td> <%= el %></td>
              <% end %>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
      <div class="col-xs-6">
      <h4>MITIE Relation</h4>
      <table class="table table-striped">
        <thead>
          <tr>
            <th>First Entity</th>
            <th>Relation Type</th>
            <th>Second Type</th>
          </tr>
        </thead>

        <tbody>
          <% @response2['mitie_relations'].each do |r| %>
            <tr>
                <% r.each do |el| %>
                <td> <%= el %></td>
              <% end %>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
    </div>
    <br>
  </div>
</div>

